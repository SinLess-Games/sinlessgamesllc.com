version: '3.8'
services:
  gateway:
    image: sinless777/gateway:latest

  nginx:
    image: nginx:latest
    volumes:
      - ./conf.d:/etc/nginx/conf.d
    depends_on:
      - gateway
    ports:
        - 7443:7443